- Note that render (as copying OpenGL basically) is going from right-handed coordindate system to a left-handed on perspective transformation (Clip-space)
- Currently using blinns perspective transformation; might change to OpenGL depending on how blinns clipping algorithm works out...